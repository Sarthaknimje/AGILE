version: "3.9"
services:
  api:
    build: .
    image: agile-spm-ml:latest
    command: ["uvicorn", "app.api:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"

  dashboard:
    build: .
    image: agile-spm-ml:latest
    environment:
      - PYTHONPATH=/app
    command: ["streamlit", "run", "app/dashboard.py", "--server.port", "8501", "--server.address", "0.0.0.0"]
    ports:
      - "8501:8501"
